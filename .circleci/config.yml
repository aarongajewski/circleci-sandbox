version: 2.1
orbs:
  gcp-kms: carecloud/gcp-kms@0.0.1
jobs:
  build:
    docker:
      - image: google/cloud-sdk
    steps:
    - checkout
    - gcp-kms/gcr-auth
    - gcp-kms/get-decrypt:
        ciphertext-file: "example file name to decipher"
        plaintext-file: "example file name to store decipered file"
        object-path: "example gcs object path"
workflows:
  example-workflow:
    jobs:
      - build:
          context: myContext
